import { DeleteResult, UpdateResult } from './types';
import * as mongoose from 'mongoose';
export declare class Mongoose<T> {
    private model;
    constructor(model: mongoose.Model<T>);
    static connect(uri: string, options: mongoose.ConnectOptions): void;
    count(filter: mongoose.FilterQuery<T>): Promise<number>;
    create(doc: mongoose.AnyObject): Promise<mongoose.HydratedDocument<T> | undefined>;
    deleteOne(condition: mongoose.FilterQuery<T>): Promise<DeleteResult>;
    findById(id: string): Promise<mongoose.HydratedDocument<T> | null>;
    findOne(condition: mongoose.FilterQuery<T>): Promise<mongoose.HydratedDocument<T> | null>;
    updateOne(condition: mongoose.FilterQuery<T>, update: mongoose.UpdateQuery<T>): Promise<UpdateResult>;
}
